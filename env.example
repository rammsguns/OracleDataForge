# Backend listener. 0.0.0.0 makes the production site available on the local network.
# Set to 127.0.0.1 to keep it accessible only from this machine.
HOST=0.0.0.0
PORT=3001

# Use production when serving the built SPA from the backend.
NODE_ENV=development
